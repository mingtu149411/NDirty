# Codex 提示词：P0 工程初始化

你正在 `D:\xuexishiyong\水印项目\NDirty` 开发离线 Windows 软件 NDirty。请只完成 P0：建立可重复、可测试的工程骨架，不下载模型、不实现图像修补、不创建 EXE。

效果优先基线：工程结构和测试约定必须支持后续以原图像素坐标、保比例 ROI、真实模型回归样例衡量补全质量；不得为了早期简化固定任何会导致整图变形的接口。

硬性约束：所有交付文件必须位于 NDirty；最终发行包预算不超过 10GB；应用必须离线可运行；不得修改或删除用户已有文件；使用 `apply_patch` 编辑文本；不添加未审查的大型依赖。

请执行：
1. 检查现有仓库和 `.gitignore`，保留已有无关改动。
2. 建立 `src/ndirty` 的 src-layout，包含 `app`、`ui`、`domain`、`services`、`engines`、`infra` 包及合理的 `__init__.py`。
3. 添加 `pyproject.toml`，仅声明 P0/P1 必需依赖：PySide6、Pillow、NumPy、pytest；不要在此阶段加入 OCR、SAM、torch 或模型。
4. 添加最小可启动 PySide6 窗口和 `python -m ndirty` 入口；窗口显示产品名、离线状态和未打开文档状态。
5. 添加单元测试，验证导入应用核心模块及文档空状态。
6. 添加 `.gitignore`，忽略 `.venv`、缓存、`build/`、`dist/`、日志、临时目录和实际模型文件，但保留模型清单。
7. 添加 `项目状态.md`、`第三方清单.md` 和 `scripts/check_size.ps1`。体积脚本须计算 NDirty 发布相关文件大小并在超过 9GB 时返回非零退出码。
8. 运行最适合的静态检查/测试；若缺少依赖，只报告精确命令，不擅自进行网络下载。

验收：给出变更文件列表、测试结果、尚未完成项及体积检查结果。不要超出 P0 范围。
