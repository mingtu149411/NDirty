# Codex 提示词：P5 可靠性、隐私与离线验收

请审查并完善现有 NDirty，不新增大型功能或模型。

效果优先约束：把固定质量样例加入离线回归；核验非蒙版区域逐像素不变、结果视图不显示编辑蒙版，并保留每个模型/预处理变更的效果对比。

完成：
1. 增加诊断页：应用版本、Python/ONNX provider、模型是否存在、模型哈希、可用内存与发布目录体积；不发送任何网络请求。
2. 统一异常处理和日志：错误信息对用户可读，技术堆栈只写本地日志；日志不得包含图片像素或图像副本。
3. 覆盖异常：无模型、损坏图片、只读输出路径、磁盘空间不足、超尺寸图片、取消推理、程序中断后的临时文件清理。
4. 补齐 `.ndirty` 项目保存/恢复和未保存更改提示；不得覆盖原图。
5. 编写并执行离线验收清单：物理断网/阻断网络后，启动、打开、编辑、补全、保存项目、导出皆成功。
6. 执行包大小、许可证、第三方清单、SHA-256 校验；修复所有阻断问题。

验收输出必须包含测试矩阵、通过/失败结果、已知限制、发布目录精确大小。不要打包 EXE，本阶段专注可用性与可审计性。
