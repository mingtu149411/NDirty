Metadata-Version: 2.4
Name: ndirty
Version: 0.1.0
Summary: Offline local image occlusion completion workspace
Author: NDirty contributors
License: Proprietary
Requires-Python: <3.14,>=3.11
Description-Content-Type: text/markdown
Requires-Dist: PySide6==6.8.3
Requires-Dist: Pillow==11.0.0
Requires-Dist: numpy==2.1.3
Provides-Extra: dev
Requires-Dist: pytest==8.3.3; extra == "dev"

# NDirty

NDirty 是计划中的 Windows 离线图像遮挡补全软件。首版通过用户编辑蒙版，并使用本地修补模型生成与周边一致的内容；不会上传图片，也不承诺恢复被遮挡区域的真实原始像素。

当前处于 P0 工程初始化阶段，尚未包含任何模型或图像补全能力。

## 开发环境（待安装 Python 后）

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e ".[dev]"
python -m pytest
python -m ndirty
```

要求 Python 3.11–3.13 x64。最终发行版本会携带所需运行时，最终用户不需要安装 Python。

## 合规与隐私

请只处理您拥有权利或已获得授权的图像。软件设计为本地离线运行，原图默认只读，导出结果必须另存。
