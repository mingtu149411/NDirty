# P6 Windows 便携版发布验收

构建日期：2026-09-02；版本：0.1.1；目标：Windows x64 离线便携版。

## 交付物

- 发行目录：`dist/NDirty/`
- 启动程序：`dist/NDirty/NDirty.exe`
- EXE SHA-256：`7530d0163efaaa6598acca141c190e83a92b1f42e69dca50bc16b7ad480910f7`
- 完整文件哈希：`dist/NDirty/SHA256SUMS.txt`（732 项）
- 精确大小：448,118,221 字节（0.4173 GiB），低于 9 GiB 警戒线和 10 GiB 上限。

## 构建与验证

| 项目 | 结果 |
|---|---|
| 固定构建器 | PyInstaller 6.16.0，Python 3.13.9 x64 |
| 资源 | 内置 LaMa、项目指定的 OCR 权重、Qt/ONNX/OpenCV 运行时、指南、通知、版本与哈希清单 |
| 独立启动 | 通过：直接运行 NDirty 0.1.1 的 `NDirty.exe` 4 秒后进程持续运行 |
| 开发环境依赖 | 发布目录不含 `.venv`、pip 缓存、测试或源码目录 |
| 模型位置 | `_internal/models/lama` 与 `_internal/models/ocr` 均存在 |
| 体积审计 | 通过：`scripts/check_size.ps1 -ProjectRoot dist/NDirty` 返回成功 |
| 固定质量回归 | 开发环境 P5 已通过 17 项测试；P6 仅完成当前 Windows 构建机的启动验证，尚无独立干净 Windows 虚拟机 |

## 已知限制与发布条件

- 需要在未安装 Python 的干净 Windows 环境完成导入、蒙版、LaMa 推理、项目保存和导出的人工验收，才可声明为已完成跨环境发布验证。
- PP-OCR 模型版权归 Baidu。发行包随附 `THIRD_PARTY_NOTICES.md`，但公开或商业分发前，发布者仍须根据上游条款核验模型再分发许可。
- 若需回滚，保留当前 `dist/NDirty` 目录或用 `SHA256SUMS.txt` 验证备份；重新运行 `scripts/build_release.ps1` 可重建同一版本。
