# NDirty 项目状态

项目创建者：**南京大学智能化软件专业241880428沈逸文**

更新时间：2026-09-02

## 当前阶段

- 阶段：P6 Windows 便携 EXE 构建（已完成，待干净 Windows 人工验收与 OCR 权重许可复核后公开发布）
- 代码骨架：已建立并验证
- 开发解释器：Python 3.13.9 x64（位于已安装软件的 Python SDK，创建 NDirty 专用 `.venv`）
- 依赖：已安装并通过 `pip check`；PySide6 由 6.8.0 调整为支持 Python 3.13 的 6.8.3
- 单元测试：2026-09-02，17 passed（含 LaMa 输入输出合成、模型缺失诊断、原图保护导出、GrabCut 智能选区与本地诊断）
- GUI 烟雾测试：2026-09-02，使用 `QT_QPA_PLATFORM=offscreen` 创建、显示并自动退出 `MainWindow` 成功
- P1 GUI 导入验收：2026-09-02，临时 PNG 导入、元数据展示、缩放和原图 SHA-256 不变均已通过
- P2 GUI 验收：2026-09-02，离屏环境中导入图像、编辑蒙版、撤销均已通过
- P3 实际推理验收：2026-09-02，CPU 离线 LaMa 对 128×96 合成遮挡图补全成功，耗时约 11 秒；输出尺寸和非蒙版区域均正确
- P3 GUI 验收：2026-09-02，补全相关控件在离屏 Qt 环境可创建
- 效果优先复审：2026-09-02，修正了整图非等比缩放问题；现使用蒙版中心的保比例方形 ROI、反射补边和蒙版内合成，结果视图自动隐藏编辑蒙版
- 修改后 P0–P3 全链路回归：2026-09-02，`pip check`、15 项单元测试、离线导入、蒙版编辑、`.ndirty` 保存恢复、真实 LaMa 推理、结果预览和安全导出均通过；160×96 合成样例 CPU 推理耗时约 11.32 秒，原图 SHA-256 与非蒙版像素保持不变
- 体积审计：2026-09-02，725,431,151 字节（约 0.6756 GB，低于 9 GB 警戒线）
- P4 OCR 验收：2026-09-02，RapidOCR 已锁定项目内 `models/ocr/` 权重目录，在离线本地模型路径初始化并识别合成文字图中的 1 个候选
- P4 智能选区验收：2026-09-02，离线 GrabCut 框选候选生成、确认合并与 GUI 工具栏烟雾测试通过；候选预览不会直接改写蒙版
- 体积审计：2026-09-02，约 0.858 GB（含开发 `.venv`，低于 9 GB 警戒线；发行包不纳入 `.venv`）
- P5 诊断与稳定性：2026-09-02，本地诊断窗口已显示版本、Python、ONNX provider、LaMa 模型存在状态/哈希、目录大小和磁盘空间；技术日志只写本地且不包含图像内容；关闭时会提示未保存的蒙版；LaMa 采用可用 CUDA 优先、CPU 必定回退策略
- P5 离线回归：2026-09-02，17 项单元测试、`pip check`、离屏 GUI 烟雾测试和 LaMa SHA-256 核验均通过；完整矩阵见 `docs/P5离线验收矩阵.md`
- P6 构建验收：2026-09-02，使用 PyInstaller 6.16.0 构建 `dist/NDirty/NDirty.exe`；修复首次构建入口未调用 `main()` 的问题后，NDirty 0.1.1 EXE 直接启动并保持运行；完整发行包 448,118,221 字节（0.4173 GiB），EXE SHA-256 为 `7530d0163efaaa6598acca141c190e83a92b1f42e69dca50bc16b7ad480910f7`
- 流程体验修订：2026-09-02，参考 ATex 的阶段化工作台，NDirty 0.1.1 新增首屏引导、左侧四步进度与醒目的“补全图像”主按钮；项目/蒙版持久化工具移至辅助菜单，蒙版修改自动使旧结果失效

## 下一步

进入 P7：仅在自有/已授权验收集稳定失败时，开展受控特化训练。

## 已知限制

- 当前版本支持图像导入、蒙版编辑、`.ndirty` 项目保存与恢复、LaMa 本地补全、结果对比、安全导出、OCR 文字候选与本地智能框选。
- 开发 `.venv` 只用于开发与验证，P6 打包时不会直接纳入发行目录。
- OCR 与 GrabCut 当前在 UI 线程运行；超大图上的候选生成仍可能短暂阻塞，P6 前应视打包前体验复测决定是否继续优化。
- P6 尚未在独立的未安装 Python Windows 环境完成全流程人工验收；PP-OCR 权重再分发许可也须由实际发布者在公开分发前确认。详见 `docs/P6发布验收.md`。
